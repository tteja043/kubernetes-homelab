apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: homelab
spec:
  selector:
    app: nginx
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
