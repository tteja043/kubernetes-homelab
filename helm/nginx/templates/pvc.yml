apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.storage.pvcName }}
  namespace: homelab

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi
